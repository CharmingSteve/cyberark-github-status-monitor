import json
import os
import boto3
import requests
from datetime import datetime
from main import lambda_handler  # Import the handler from main.py

# Keep the imports in case they're needed by other functions
